<html>

<head>
    <title>Hello Movies</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="./movie-styles.css">
</head>

<body>
    <div class="container-fluid text-center header">
        <div class="container">
            <a href="/">Hello movies</a>
            <form action='/search' method='GET' class="form-inline search-form">
                <input name='searchTerm' class="form-control search-input" placeholder="Movie Title" />
                <button type='submit' class="btn btn-warning">Search</button>
            </form>
        </div>
    </div>
    <div class="container-fluid text-center movies">
        <div class="row">
            <% for(let i=0;i<dataObject.results.length;i++) {%>
                <div class="col-12 col-sm-4 col-md-4 col-lg-4 movie-block">
                    <h3 class="movie-title">
                        <%= dataObject.results[i].title %>
                            <%- "<small>( " + parseInt(dataObject.results[i].release_date) + " )</small>" %>
                    </h3>
                    <p class="no-padding">User Score:
                        <%= (dataObject.results[i].vote_average) * 10%>%
                    </p>
                        <img src="https://image.tmdb.org/t/p/w300_and_h450_bestv2<%=dataObject.results[i].poster_path%>" class="movie-poster"
                        />
                </div>
                <% } %>
        </div>
    </div>
    <footer>
        <div class="container-fluid text-center">
            <h2>Footer</h2>
        </div>
    </footer>
</body>

</html>